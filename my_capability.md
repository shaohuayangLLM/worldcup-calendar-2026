# 本次对话调用的 MCP 工具

> 仅记录带 `mcp__` 前缀、本次对话中**实际调用过**的 MCP 工具。
> 内置工具（Bash / Write / Edit / Read / WebSearch / WebFetch / TaskCreate / TaskUpdate / TaskList / Skill / ToolSearch / AskUserQuestion）不属于 MCP，未列入。

## Playwright（浏览器自动化，用于注册 football-data.org 账号）
- mcp__playwright__browser_navigate
- mcp__playwright__browser_snapshot
- mcp__playwright__browser_fill_form
- mcp__playwright__browser_click

---

合计：实际调用的 MCP 工具 4 个（均来自 playwright server）。
